[pytest]
pythonpath = .
markers =
    slow: marks tests as slow/heavy integration tests (Airflow, PySpark, PyTorch, LangGraph, Dataflow)
    integration: marks integration tests requiring DB/mock services
    unit: marks fast unit tests